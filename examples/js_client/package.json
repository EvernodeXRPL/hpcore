{
    "scripts": {
        "merge-browser": "mkdir -p dist && cp bson-browser.js dist/hp-client-lib.js && cat hp-client-lib.js >> dist/hp-client-lib.js",
        "minify-browser": "minify dist/hp-client-lib.js > dist/hp-client-lib.min.js && rm dist/hp-client-lib.js",
        "build-browser": "npm run merge-browser && npm run minify-browser"
    },
    "dependencies": {
        "libsodium-wrappers": "0.7.6",
        "blake3": "2.1.4",
        "ws": "7.1.2",
        "bson": "4.0.4"
    },
    "devDependencies": {
        "minify": "7.0.2"
    }
}
